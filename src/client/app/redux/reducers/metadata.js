const metadata = (
    state = {
        title: '',
        description: '',
        keywords: '',
        canonical: '',
    },
    action,
) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default metadata;
